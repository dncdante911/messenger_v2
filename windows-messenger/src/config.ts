// Re-export from api.ts for backwards compatibility
// All config constants now live in api.ts
export {
  API_BASE_URL,
  NODE_BASE_URL,
  SOCKET_URL,
  REGISTER_PATH,
  WINDOWS_APP_BASE_URL,
  SITE_ENCRYPT_KEY,
  SERVER_KEY,
  TURN_FALLBACK
} from './api';
